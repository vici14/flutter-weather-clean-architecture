import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF2A2A2A);
  static const Color accent = Color(0xFF556799);

  // Text colors
  static const Color textPrimary = Color(0xFF2A2A2A);
  static const Color textSecondary = Color(0xFF556799);
  static const Color textWhite = Color(0xFFFFFFFF);

  // Background colors
  static const Color background = Colors.white;
  static const Color cardBackground = Color(0xFFF5F5F5);
  static const Color appBackground = Color(0xFFF5F6F7);

  // Error screen colors
  static const Color errorBackground = Color(0xFFE85959);
  static const Color buttonBackground = Color(0xFF4A4A4A);

  // Other colors
  static const Color divider = Color(0xFFEEEEEE);
  static const Color error = Colors.red;
}
